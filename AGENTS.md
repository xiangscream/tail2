# 工作约定

本项目验证 Tail2 中层能力与视觉 Runtime Agent。先读 `docs/handoff/CURRENT.md`，再读开发计划和 M0 操作手册。

- 用户已经确认：Target 指画面里的具体对象；Agent 必须看图；USB/UVC 为首选视觉输入；原生候选优先；应用框架后置；只做观察状态页。
- 使用同一产品能力契约。SDK 只进入 native 适配器；Agent 不获得原始 SDK 或任意命令执行工具。
- M0 是受人监督的台架探测工具。M1 的调度、观察与取消契约尚未全部落地。禁止把原始 probe JSONL 直接交给 Agent 无人运行。
- 请求成功、设备回读、画面验证、文件可取回分别记录。未测写 pending，超时写 indeterminate；模拟结果不算真机通过。
- 用户已经选择 USB。发现媒体模式冲突时报告，不静默切换网络或用截图冒充机内照片。
- SDK 包没有在本仓库附带分发授权。禁止提交 vendor、头文件、库、PDB、样例、原始 SDK 日志或内部项目文档。
- 真机图像、序列号、设备路径、候选外观描述、录制文件、凭据都存本地。公开 handoff 只写脱敏结论。
- 每轮 `git fetch`、`git status`，从最新基线新建分支。不要覆盖别人未合并的成果，不 force push，不自行合并。
- 运动测试前验证可用的停止方式，人在旁边；未知 API 不自动尝试，不修改固件、复位设备或覆盖已有预置位。
- 本地进程结束不能当作设备已停止。SDK 卡住时停止自动测试，请操作者检查设备。
- `position.*` 在 Tail2 里是云台视角预置；不要赋予它空间位置 / 移动底盘含义。
- 新节点只有确有必要时才引入；不增加 App Store、MCP、人格、记忆或通用工作流引擎。
- 交付包含测试命令、结果、运行环境、未确认项、原始日志的本地位置。禁止将 API 返回码改写成视觉成功。
